spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/chandao?zeroDateTimeBehavior=convertToNull&useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  application:
    name: chandao-report
#  devtools:
#    restart:
#      enabled: true
#  thymeleaf:
#    cache: false

server:
  port: 8090
  servlet:
    context-path: /report
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
